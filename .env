# URL base da API do backend.
# Local: http://localhost:8000
# Demo via ngrok: cole aqui a URL pública https do ngrok (ex.: https://abc123.ngrok-free.app).
# O WebSocket do chat é derivado desta URL automaticamente (ws/wss).
VITE_API_URL=http://localhost:8000
